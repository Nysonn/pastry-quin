# Neon Postgres connection string (pooled)
DATABASE_URL=

# 64-char hex secret used to sign admin session JWTs (openssl rand -hex 32)
SESSION_SECRET=

# Optional: override the admin account created by `npm run db:seed`
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
